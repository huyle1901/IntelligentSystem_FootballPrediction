# Optional API key for football-data.org features such as upcoming matches and players.
API_FOOTBALL_DATA=

# Used by Docker Compose for PostgreSQL. Leave unset for local SQLite fallback.
DATABASE_URL=
